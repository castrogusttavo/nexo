services:
  nexo-app:
    image: ghcr.io/castrogusttavo/nexo:latest
    container_name: nextjs-app
    restart: unless-stopped
    ports:
      - "3000:3000"
    env_file:
      - .env
    networks:
      - app-network

networks:
  app-network:
    external: true
    name: nexo_default
