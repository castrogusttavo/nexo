creation_rules:
  - path_regex: secrets/.*\.env$
    age: >-
      age16gwfmnsyt2nze0cgjun2zl6rn2m5cj53kmyk4ctzgm90e8q53aeqqzpxme
    # Replace the public key above with your actual age public key.
    # Generate a key pair: age-keygen -o nexo-secrets.key
    # The public key is printed to stdout during generation.
